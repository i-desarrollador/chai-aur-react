import './App.css';
import Chai from './Chai';

function App() {
  return (

    <> 
    <h2> hello mate(APP)</h2>
    <Chai/>
    </>
   
  );
}

export default App;


//keep components name  starting with capital letters, return should have only one main div or fragment, keep file name starting wiht caps but this is not necessary , also file names extension should be jsx in vite bundler but in normal node js is fine  
 